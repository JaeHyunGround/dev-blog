---
title: "React Hooks 완벽 가이드"
excerpt: "useState, useEffect부터 커스텀 훅까지, React Hooks의 모든 것을 알아봅니다."
publishedAt: "2025-01-08"
tags: ["React", "JavaScript", "Hooks"]
---

# React Hooks 완벽 가이드

React Hooks는 함수형 컴포넌트에서 상태와 생명주기 기능을 사용할 수 있게 해주는 강력한 기능입니다.

## useState

가장 기본적인 Hook으로, 컴포넌트에 상태를 추가할 수 있습니다.

```jsx
const [count, setCount] = useState(0);
```

## useEffect

컴포넌트의 생명주기와 사이드 이펙트를 관리합니다.

```jsx
useEffect(() => {
  document.title = `Count: ${count}`;
}, [count]);
```

## 커스텀 Hook

재사용 가능한 로직을 Hook으로 분리할 수 있습니다.

```jsx
function useCounter(initialValue = 0) {
  const [count, setCount] = useState(initialValue);
  const increment = () => setCount(c => c + 1);
  return { count, increment };
}
```

Hooks를 사용하면 코드의 재사용성과 가독성이 크게 향상됩니다.
